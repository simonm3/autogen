"""
This file is automatically generated by the autop package.
Please edit the marked area only. Other areas will be
overwritten when autop is rerun.
"""

from setuptools import setup

params = dict(
    name='autop',
    description='Automate development tasks',
    version='2.0.0',
    url='https://github.com/simonm3/autogen.git',
    install_requires=['autopep8', 'docopt', 'pypiwin32', 'setuptools'],
    packages=['autop'],
    package_data={},
    include_package_data=True,
    py_modules=[],
    scripts=None)

########## EDIT BELOW THIS LINE ONLY ##########

# prevent vscode moving this to top
if True:
    import sys

if sys.platform != "win32":
    print("warning. must install pypiwin32")
params["install_requires"].remove("pypiwin32")
params["install_requires"].append("twine")

# enable command line
params.update(entry_points={"console_scripts": [
              "autop = autop.autop:main"]})

########## EDIT ABOVE THIS LINE ONLY ##########

setup(**params)
